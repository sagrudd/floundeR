% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam.R
\name{bam_check_index}
\alias{bam_check_index}
\title{Check BAM index evidence for QC report cards}
\usage{
bam_check_index(path, require = FALSE, prefer_csi = FALSE)
}
\arguments{
\item{path}{Character scalar. Local BAM file to inspect.}

\item{require}{Logical scalar. Treat a missing index as a Bamana command
failure while still returning structured index evidence when available.}

\item{prefer_csi}{Logical scalar. Prefer CSI over BAI when both sidecars are
present and usable.}
}
\value{
A named list with \code{status}, \code{index}, \code{candidates}, and
\code{error} data frames.
}
\description{
\code{bam_check_index()} calls Bamana's in-process Rust index inspection path
and returns stable R tables for index presence, support level, freshness, and
candidate sidecars. It is read-only and does not create or repair indexes.
}
\examples{
\dontrun{
bam_check_index("reads.bam")
}
}
