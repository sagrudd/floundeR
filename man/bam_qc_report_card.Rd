% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_report_card.R
\name{bam_qc_report_card}
\alias{bam_qc_report_card}
\title{Build a BAM QC report card}
\usage{
bam_qc_report_card(
  summary = NULL,
  index = NULL,
  mapping = NULL,
  sorting = NULL,
  tags = NULL,
  eof = NULL,
  validation = NULL,
  provenance_anomalies = NULL,
  expected_tags = character(),
  thresholds = bam_qc_report_card_thresholds(),
  schema_version = "flounder.bam_qc_report_card.v1"
)
}
\arguments{
\item{summary}{Optional \code{bam_summary()} result.}

\item{index}{Optional \code{bam_check_index()} result.}

\item{mapping}{Optional \code{bam_check_map()} result used as a fallback for
mapping-fraction evidence when \code{summary} is absent.}

\item{sorting}{Optional \code{bam_check_sort()} result.}

\item{tags}{Optional \code{bam_check_tag()} data frame or list of such data
frames.}

\item{eof}{Optional \code{bam_check_eof()} data frame.}

\item{validation}{Optional \code{bam_validate()} result.}

\item{provenance_anomalies}{Optional numeric anomaly count or data frame of
provenance findings from a future Bamana provenance/forensic surface.}

\item{expected_tags}{Character vector of BAM aux tags expected to be present.}

\item{thresholds}{Named threshold list. Defaults to
\code{bam_qc_report_card_thresholds()}.}

\item{schema_version}{Schema version label to attach to the returned table.}
}
\value{
A tibble using the standard report-card schema:
\code{schema_version}, \code{check_id}, \code{check_label}, \code{status},
\code{observed_value}, \code{warn_threshold}, \code{fail_threshold},
\code{comparator}, and \code{details}.
}
\description{
\code{bam_qc_report_card()} evaluates Bamana-derived BAM evidence against
pass/warn/fail checks for alignment QC and reporting. It consumes the R-native
objects returned by \code{bam_summary()}, \code{bam_check_index()},
\code{bam_check_sort()}, \code{bam_check_tag()}, \code{bam_check_eof()}, and
\code{bam_validate()}; it does not call Bamana directly.
}
\examples{
\dontrun{
bam <- bam_summary("reads.bam")
bam_qc_report_card(summary = bam)
}
}
