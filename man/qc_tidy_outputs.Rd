% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_tidy_outputs.R
\name{qc_tidy_outputs}
\alias{qc_yield_over_time}
\alias{qc_read_length_distribution}
\alias{qc_quality_distribution}
\alias{qc_channel_density}
\alias{qc_barcode_composition}
\title{Build tidy QC data from sequencing-summary inputs}
\usage{
qc_yield_over_time(
  x,
  resolution_minutes = 15,
  schema_version = "flounder.qc_yield_over_time.v1"
)

qc_read_length_distribution(
  x,
  bins = 20,
  schema_version = "flounder.qc_read_length_distribution.v1"
)

qc_quality_distribution(
  x,
  bins = 20,
  schema_version = "flounder.qc_quality_distribution.v1"
)

qc_channel_density(x, schema_version = "flounder.qc_channel_density.v1")

qc_barcode_composition(
  x,
  schema_version = "flounder.qc_barcode_composition.v1"
)
}
\arguments{
\item{x}{A \code{SequencingSummary} object, a sequencing-summary file path, or a
data frame/tibble with floundeR-normalised sequencing-summary columns.}

\item{resolution_minutes}{Temporal bin size in minutes.}

\item{schema_version}{Schema version label to attach to the returned table.}

\item{bins}{Number of distribution bins to produce.}
}
\value{
\code{qc_yield_over_time()} returns schema version, bin bounds,
pass/fail state, read count, base yield, and cumulative read/base yield.

\code{qc_read_length_distribution()} returns schema version, read-length bin
bounds, pass/fail state, read count, and base yield.

\code{qc_quality_distribution()} returns schema version, Q-score bin bounds,
pass/fail state, read count, and base yield.

\code{qc_channel_density()} returns schema version, channel, read/base yield,
pass/fail read counts, and pass fraction.

\code{qc_barcode_composition()} returns schema version, barcode arrangement,
read/base yield, pass/fail read counts, and read/base fractions.
}
\description{
These helpers return stable, schema-versioned tibbles for the first
floundeR QC plot and reporting families. They accept the same input shapes
as \code{qc_run_summary()}: a \code{SequencingSummary} object, a
sequencing-summary file path, or a normalised data frame/tibble.
}
\examples{
summary_file <- flnDr("sequencing_summary.txt.bz2")
qc_yield_over_time(summary_file)
qc_read_length_distribution(summary_file, bins = 10)
qc_quality_distribution(summary_file, bins = 10)
qc_channel_density(summary_file)
qc_barcode_composition(summary_file)
}
