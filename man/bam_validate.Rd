% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam.R
\name{bam_validate}
\alias{bam_validate}
\title{Validate BAM structure and selected consistency checks}
\usage{
bam_validate(
  path,
  max_errors = 100L,
  max_warnings = 100L,
  header_only = FALSE,
  records = NULL,
  fail_fast = FALSE,
  include_warnings = TRUE
)
}
\arguments{
\item{path}{Character scalar. Local BAM file to validate.}

\item{max_errors}{Positive integer scalar. Maximum error findings to retain.}

\item{max_warnings}{Non-negative integer scalar. Maximum warning findings to
retain.}

\item{header_only}{Logical scalar. Validate the header only.}

\item{records}{Optional positive integer scalar. Validate at most this many
records after the header.}

\item{fail_fast}{Logical scalar. Stop at the first error-level finding.}

\item{include_warnings}{Logical scalar. Include warning-level findings.}
}
\value{
A named list with \code{status}, \code{summary}, \code{findings}, and
\code{error} data frames. \code{status$ok} preserves Bamana's command envelope
result; a validation failure with findings therefore returns \code{ok = FALSE}
rather than raising an R error.
}
\description{
\code{bam_validate()} calls Bamana's structural validation library path
in-process. Validation findings are returned as evidence tables even when
Bamana reports validation failure; missing or unreadable inputs without a
validation payload still raise typed Bamana-backed R conditions.
}
\examples{
\dontrun{
bam_validate("reads.bam")
}
}
