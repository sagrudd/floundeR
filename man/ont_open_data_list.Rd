% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ont_open_data.R
\name{ont_open_data_list}
\alias{ont_open_data_list}
\alias{flounder_ont_zymo_pod5_prefix}
\title{List objects in an ONT open-data S3 prefix}
\usage{
ont_open_data_list(
  prefix = flounder_ont_zymo_pod5_prefix(),
  bucket = "ont-open-data",
  region = "eu-west-1",
  max = 1000L,
  anonymous = TRUE
)

flounder_ont_zymo_pod5_prefix()
}
\arguments{
\item{prefix}{Character scalar. S3 object prefix to list.}

\item{bucket}{Character scalar. Public S3 bucket name.}

\item{region}{Character scalar. AWS region for the bucket.}

\item{max}{Integer scalar. Maximum number of objects to return.}

\item{anonymous}{Logical scalar. If \code{TRUE}, temporarily sets
\code{AWS_NO_SIGN_REQUEST=true} while listing.}
}
\value{
A data frame with one row per object and the columns \code{bucket}, \code{key},
\code{size}, \code{last_modified_utc}, \code{etag}, and \code{storage_class}.
}
\description{
\code{ont_open_data_list()} performs an explicit anonymous listing of a public ONT
open-data S3 prefix. The default prefix is the selected Zymo fecal POD5
source used by floundeR integration examples. The helper lists metadata only;
it does not download POD5 files.
}
\examples{
\dontrun{
if (identical(tolower(Sys.getenv("FLOUNDER_RUN_NETWORK_TESTS")), "true")) {
  ont_open_data_list(max = 5)
}
}

}
