% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pod5.R
\name{pod5_manifest}
\alias{pod5_manifest}
\title{Build a versioned POD5 collection manifest}
\usage{
pod5_manifest(path)
}
\arguments{
\item{path}{Character scalar. POD5 file or directory to inventory.}
}
\value{
A data frame with one row per POD5 file and the columns
\code{schema_version}, \code{source}, \code{relative_path}, \code{path}, \code{size_bytes},
\code{verification_status}, and \code{verification_failed_checks}.
}
\description{
\code{pod5_manifest()} inventories a local POD5 file, folder, or run tree through
the in-process Rust extension and the curated \code{pod5-tools} library API. The
returned manifest is intentionally row-oriented for R and synoptikon handoff:
every row carries the manifest schema version and source path alongside one
POD5 entry.
}
\examples{
signature <- as.raw(c(0x8b, 0x50, 0x4f, 0x44, 0x0d, 0x0a, 0x1a, 0x0a))
run_dir <- file.path(tempdir(), "flounder-manifest-example")
unlink(run_dir, recursive = TRUE)
dir.create(run_dir, recursive = TRUE)
writeBin(c(signature, as.raw(rep(0, 16)), signature), file.path(run_dir, "a.pod5"))
pod5_manifest(run_dir)

}
