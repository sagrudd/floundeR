% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ont_open_data.R
\name{ont_open_data_fetch}
\alias{ont_open_data_fetch}
\title{Fetch one ONT open-data object into an explicit cache directory}
\usage{
ont_open_data_fetch(
  key,
  cache_dir = tools::R_user_dir("floundeR", which = "cache"),
  bucket = "ont-open-data",
  region = "eu-west-1",
  file_name = NULL,
  overwrite = FALSE,
  anonymous = TRUE
)
}
\arguments{
\item{key}{Character scalar. Full S3 object key to download.}

\item{cache_dir}{Character scalar. Directory where the object should be
cached. Defaults to the user cache for \code{floundeR}.}

\item{bucket}{Character scalar. Public S3 bucket name.}

\item{region}{Character scalar. AWS region for the bucket.}

\item{file_name}{Optional character scalar. Local file name inside
\code{cache_dir}. Defaults to \code{basename(key)}.}

\item{overwrite}{Logical scalar. If \code{FALSE} and the local file already
exists, return metadata without downloading again.}

\item{anonymous}{Logical scalar. If \code{TRUE}, temporarily sets
\code{AWS_NO_SIGN_REQUEST=true} while listing and downloading.}
}
\value{
A one-row data frame with \code{bucket}, \code{key}, \code{size},
\code{last_modified_utc}, \code{etag}, \code{storage_class}, \code{cache_path}, and
\code{downloaded}.
}
\description{
\code{ont_open_data_fetch()} downloads exactly one selected public ONT open-data
object. It never expands a prefix into multiple downloads. Call
\code{\link[=ont_open_data_list]{ont_open_data_list()}} first when object discovery is required, then pass a
single object key to this function.
}
\examples{
\dontrun{
if (identical(tolower(Sys.getenv("FLOUNDER_RUN_NETWORK_TESTS")), "true")) {
  ont_open_data_fetch(
    key = paste0(
      flounder_ont_zymo_pod5_prefix(),
      "PAU85136_pass_279c9095_68316534_8289.pod5"
    ),
    cache_dir = file.path(tempdir(), "flounder-ont-open-data")
  )
}
}

}
