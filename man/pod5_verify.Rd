% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pod5.R
\name{pod5_verify}
\alias{pod5_verify}
\title{Verify a local POD5 candidate file}
\usage{
pod5_verify(path)
}
\arguments{
\item{path}{Character scalar. Candidate POD5 file to verify.}
}
\value{
A data frame with one row per verification check and the columns
\code{path}, \code{size_bytes}, \code{overall_status}, \code{check}, \code{category}, \code{status}, and
\code{detail}.
}
\description{
\code{pod5_verify()} checks a single local file through the in-process Rust
extension and the curated \code{pod5-tools} library API. The current backend
performs fast extension and fixed POD5 signature checks, then reports deeper
layout/schema checks as \code{not_checked} until the full parser backend is
connected. A signature-valid file therefore returns \code{overall_status =
"incomplete"} rather than overstating full POD5 validity.
}
\examples{
signature <- as.raw(c(0x8b, 0x50, 0x4f, 0x44, 0x0d, 0x0a, 0x1a, 0x0a))
path <- file.path(tempdir(), "flounder-signature-example.pod5")
writeBin(c(signature, as.raw(rep(0, 16)), signature), path)
pod5_verify(path)

}
