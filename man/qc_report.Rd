% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grammateus.R
\name{qc_report}
\alias{qc_report}
\title{Assemble a Grammateus-backed QC report contract}
\usage{
qc_report(
  elements,
  figures = NULL,
  output_dir,
  output = c("html", "pdf"),
  render = c("if_available", "never", "require"),
  theme = grammateus_mnemosyne_theme(),
  report_id = "report_nanopore_qc",
  title = "Nanopore sequencing QC report",
  produced_by = "floundeR",
  producer_version = NULL,
  produced_at_utc = Sys.time(),
  run_id = NULL,
  overwrite = TRUE
)
}
\arguments{
\item{elements}{A single Grammateus report element, a
\code{flounder_grammateus_report_element_bundle}, or a named list of report
elements.}

\item{figures}{Optional governed figure, figure bundle, or list of governed
figures prepared by \code{grammateus_figure_from_file()} or
\code{grammateus_figure_from_ggplot()}.}

\item{output_dir}{Directory where the report contract and manifest are
written.}

\item{output}{Requested rendered formats. Use any of \code{html} and \code{pdf}.}

\item{render}{Render policy: \code{if_available} records unavailable render
outputs when the private runtime is absent, \code{never} writes only the
contract and manifest, and \code{require} errors unless rendering is available.}

\item{theme}{A \code{flounder_grammateus_theme} object.}

\item{report_id}{Stable lower-snake-case report identifier. Must start with
\code{report_}.}

\item{title}{Human-readable report title.}

\item{produced_by}{Producing software or service name.}

\item{producer_version}{Producing software version.}

\item{produced_at_utc}{Production timestamp.}

\item{run_id}{Optional upstream run, workflow, or analysis identifier.}

\item{overwrite}{Whether existing report artifact paths may be replaced.}
}
\value{
A list with class \code{flounder_qc_report} containing the themed report
contract, manifest, contract path, manifest path, requested output status,
and provenance.
}
\description{
\code{qc_report()} is the high-level floundeR report assembly API. It combines
prepared Grammateus semantic report elements, optional governed figures, and
a Mnemosyne Biosciences theme descriptor into a stable report contract and
manifest. The public package always writes the contract and manifest without
requiring private Grammateus assets. HTML/PDF rendering is attempted only
when requested and an authorized Grammateus runtime is available; otherwise
the manifest records an explicit render status.
}
