% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pod5.R
\name{pod5_compare}
\alias{pod5_compare}
\title{Compare two POD5 collections or manifests}
\usage{
pod5_compare(left, right)
}
\arguments{
\item{left, right}{Character scalars. POD5 inputs or manifest JSON files to
compare.}
}
\value{
A data frame with the columns \code{status}, \code{kind}, \code{relative_path},
\code{left_size_bytes}, \code{right_size_bytes}, \code{left_verification_status}, and
\code{right_verification_status}. Matching inputs return one row with
\code{status = "match"} and \code{kind = "match"}.
}
\description{
\code{pod5_compare()} compares two local POD5 files, folders, run trees, or
\code{pod5-tools} manifest JSON files through the in-process Rust extension. It is
designed for operational handoff checks: missing files and changed file-size
or verification-state differences are returned as rows rather than rendered
text.
}
\examples{
signature <- as.raw(c(0x8b, 0x50, 0x4f, 0x44, 0x0d, 0x0a, 0x1a, 0x0a))
left <- file.path(tempdir(), "flounder-compare-left")
right <- file.path(tempdir(), "flounder-compare-right")
unlink(c(left, right), recursive = TRUE)
dir.create(left, recursive = TRUE)
dir.create(right, recursive = TRUE)
writeBin(c(signature, as.raw(rep(0, 16)), signature), file.path(left, "a.pod5"))
writeBin(c(signature, as.raw(rep(1, 16)), signature), file.path(right, "a.pod5"))
pod5_compare(left, right)

}
